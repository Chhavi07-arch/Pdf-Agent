--extra-index-url https://download.pytorch.org/whl/cpu

# Web framework
fastapi==0.115.5
# Pin Starlette explicitly: fastapi 0.115.5 requires >=0.40.0,<0.42.0.
# Without this pin pip may install Starlette 1.0.0, which removed the
# `on_startup` Router argument FastAPI still passes → TypeError at app init.
starlette==0.41.3
uvicorn[standard]==0.32.1

# Mistral API (OpenAI-compatible REST endpoint — plain requests, no SDK needed)
requests==2.32.3

# PDF parsing
PyMuPDF==1.24.13

# Embeddings
sentence-transformers==3.3.1
torch==2.4.0
numpy==1.26.4

# Vector database
qdrant-client==1.12.1

# Keyword search for hybrid retrieval (BM25 fused with semantic search)
rank-bm25==0.2.2

# Text splitting (used in chunking layer — installed now, wired in next step)
langchain-text-splitters==0.3.8

# Utilities
python-multipart==0.0.12   # required for FastAPI file uploads
python-dotenv==1.0.1
pydantic==2.10.3
